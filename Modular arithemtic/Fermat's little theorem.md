This theorem states that if `p` is a prime number, then for any integer `a` the number $a^p - a$ is an integer multiple of `p`.
$$a^p \equiv a (\textrm{mod}\ p)$$
If `a` is not divisible by `p`:
$$a^{p-1} \equiv 1 (\textrm{mod}\ p)$$